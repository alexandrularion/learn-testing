// Exercise 4: toContain
// Create a test that checks if the array "shoppingList" contains 'milk'.
describe("toContain Matcher", () => {
  test('checks if shoppingList contains "milk"', () => {
    const shoppingList = ["bread", "milk", "eggs"];
    // Your test code here
  });
});
